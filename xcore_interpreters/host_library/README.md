# Python Bindings for x86 Interpreter (libxcore_interpreters)

This project builds libxcore_interpreters, a set of contains Python bindings for the XCOREInterpreter class.

## Building

Make a directory for the build

    > mkdir build
    > cd build

Run cmake

    > cmake ../
    > make

To install

    > make install
